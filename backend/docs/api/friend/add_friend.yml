info:
  name: add_friend
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/friend/add-friend/:username"
  params:
    - name: username
      value: durvesh
      type: path
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
